import React, { Component } from 'react';

class Questions extends Component {
  componentDidMount() {
    // this.props.getQuestionsByDimension(this.props.id);
  }
  render() {
    return (
      <h1>Hello from Questions</h1>
    );
  }
}

export default Questions;
